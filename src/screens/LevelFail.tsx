import {
  SafeAreaProvider,
  SafeAreaView
} from 'react-native-safe-area-context'

const LevelFail = () => {
  return (
    <SafeAreaProvider>
      <SafeAreaView>
      </SafeAreaView>
    </SafeAreaProvider>
  )
}

export default LevelFail