import { SafeAreaProvider, SafeAreaView } from 'react-native-safe-area-context'

const LevelSuccess = () => {
  return (
    <SafeAreaProvider>
      <SafeAreaView>
      </SafeAreaView>
    </SafeAreaProvider>
  )
}

export default LevelSuccess